---
- name: Ensure nocodb app folder exists
  file:
    mode: "0700"
    path: "{{ nocodb_app.base_path }}"
    state: directory

- name: Template docker-compose configuration
  template:
    src: "templates/docker-compose.yml.j2"
    dest: "{{ nocodb_app.base_path }}/docker-compose.yml"
    owner: "{{ ansible_user_id }}"
    group: "{{ ansible_user_gid }}"
    mode: "0644"

- name: Template nginx host
  become: true
  template:
    src: templates/nginx.conf.j2
    dest: /etc/nginx/sites-available/nocodb.conf
    owner: "root"
    group: "root"
    mode: "0644"

- name: Enable nginx host
  become: true
  ansible.builtin.file:
    src: /etc/nginx/sites-available/nocodb.conf
    dest: /etc/nginx/sites-enabled/nocodb.conf
    owner: root
    group: root
    state: link

- name: Add domain to certificate to be generated
  set_fact:
    certbot_certs: '{{ certbot_certs + [ { "domains": [nocodb_app.domain] } ] }}'

- name: Template nocodb backup configuration
  become: true
  template:
    src: templates/nocodb.yaml.j2
    dest: /etc/borgmatic.d/nocodb.yaml
    owner: root
    group: root
    mode: "0600"
    validate: /root/.local/bin/validate-borgmatic-config -c %s

# - name: Enable nocodb docker service
#   become: true
#   systemd:
#     state: started
#     enabled: false
#     name: dc@nocodb.service
