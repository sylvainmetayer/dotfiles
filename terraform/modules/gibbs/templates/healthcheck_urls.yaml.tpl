---
healthchecks_urls:
  betisier: ${betisier_url}
  crm: ${crm_url}
  nextcloud: ${nextcloud_url}
  rss: ${rss_url}
  grocy: ${grocy_url}
  nocodb: ${nocodb_url}

crm_cron_healthcheck_url: ${cron_crm}
