---
- name: Reload udev rules
  become: true
  ansible.builtin.command: udevadm control --reload-rules
  changed_when: true

- name: Restart xremap
  ansible.builtin.systemd_service:
    name: xremap
    scope: user
    daemon_reload: true
    state: restarted
  when: xremap_service_enabled | bool
